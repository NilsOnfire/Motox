<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.motox">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Motox">
        <activity
            android:name=".MainNavPasajeroAct"
            android:exported="false"
            android:label="@string/title_activity_main_nav_pasajero"
            android:theme="@style/Theme.Motox.NoActionBar" />
        <activity
            android:name=".conductor.RegistroConductorAct"
            android:exported="false" />
        <activity
            android:name=".pasajero.RegistroPasajeroAct"
            android:exported="false" />
        <activity
            android:name=".pasajero.PasajeroMapAct"
            android:exported="false"
            android:label="@string/title_activity_pasajero_map" />
        <!--
             TODO: Before you run your application, you need a Google Maps API key.

             To get one, follow the directions here:

                https://developers.google.com/maps/documentation/android-sdk/get-api-key

             Once you have your API key (it starts with "AIza"), define a new property in your
             project's local.properties file (e.g. MAPS_API_KEY=Aiza...), and replace the
             "YOUR_API_KEY" string in this file with "${MAPS_API_KEY}".
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDtYUwl_66_ix1flW3us82_BsNnIe5eMgA" />

        <activity
            android:name=".conductor.ConductorMapAct"
            android:exported="false"
            android:label="@string/title_activity_conductor_map" />
        <activity
            android:name=".MapActivity"
            android:exported="false" />
        <activity
            android:name=".ConductorLoginAct"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>